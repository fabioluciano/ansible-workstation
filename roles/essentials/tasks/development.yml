---
- name: Development | Include related tasks
  include_tasks: "{{ development_tasks }}.yml"
  loop:
    - altair
    - python
    - cloud_provider_cli
    - composer
    - dbeaver
    - nvm
    - postman
    - sdkman
    - spacevim
    - hashicorp
  loop_control:
    loop_var: development_tasks